<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="320dp"
    android:layout_height="400dp"
    android:background="@drawable/bg_text_box"
    android:padding="16dp">

    <!-- 우측 상단 닫기 버튼 (디자인 일관성) -->
    <ImageView
        android:id="@+id/btn_close_detail"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentEnd="true"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:background="@android:color/transparent"
        android:padding="4dp" />

    <!-- 상세 내용이 담기는 흰색 내부 박스 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/btn_close_detail"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_history_detail_inner"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- 선택한 질문 제목 -->
        <TextView
            android:id="@+id/tv_detail_question"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="질문 내용이 여기에 표시됩니다."
            android:textColor="#000000"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="12dp"/>

        <!-- 구분선 -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#DDDDDD"
            android:layout_marginBottom="12dp"/>

        <!-- 답변 내용 (스크롤 가능) -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fadeScrollbars="false"
            android:scrollbarThumbVertical="@android:color/darker_gray">

            <TextView
                android:id="@+id/tv_detail_answer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="답변 내용이 여기에 표시됩니다. 내용이 길어지면 자동으로 스크롤이 생성됩니다."
                android:textColor="#333333"
                android:textSize="14sp"
                android:lineSpacingExtra="4dp"/>
        </ScrollView>
    </LinearLayout>
</RelativeLayout>